paths-ignore:
  # Public "TVs and Limited Input devices" OAuth client for the HarangApp GCP
  # project. Google's own docs state this client secret isn't treated as
  # confidential for installed-app client types - it's meant to ship in
  # public source. See src/google/deviceAuth.ts for the full rationale.
  - "src/google/deviceAuth.ts"
